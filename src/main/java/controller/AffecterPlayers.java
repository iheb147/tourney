def add_numbers(a, b)
    # Oublie le deux-points et indentation incorrecte
result = a + b
return result

def greet(name):
print("Hello " + Name)  # Mauvais nom de variable (Name vs name)

def divide(x, y):
    return x / y

print(add_numbers(5, 10))
print(greet("Alice"))
print(divide(10, 0))  # Division par zéro

class Person
    def __init__(self, name, age)
        self.name = name
        self.age = age
    
    def birthday(self)
        self.age += 1
        print(f"Happy birthday {self.name}! Now you are {self.age}")

p = Person("Bob", 30)
p.birthday()
